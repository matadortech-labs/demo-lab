# Universal Orchestrator Components

`source/EndpointValidationCustomJob/` contains the custom job extension source. `scripts/` contains the profile-driven endpoint validation pipeline. `config/` and `templates/` contain public-safe example configuration.

Recreate API credentials and the SSH private key locally on the orchestrator. Neither belongs in source control.
